import React, {Component} from 'react';

const WeatherButton1 = (props) => {
    return(
        <button type='submit'  className="get-weather-btn btn-main">Get Weather By Zip</button>
        )
}

export default WeatherButton1;